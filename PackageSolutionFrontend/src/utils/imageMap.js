/* ================================================================
   imageMap.js — DEPRECATED
   ================================================================
   Snapshot images and their descriptions are now managed directly
   inside src/data/dashboardsData.js

   Each dashboard entry has a `snapshots` array:
     snapshots: [
       { image: 'FolderName/snapshot1.png', description: '...' },
       ...
     ]

   Drop your images into:
     src/assets/snapshots/<Dashboard-Folder>/snapshot1.png
================================================================ */

// This file is kept for backward compatibility but is no longer imported.
export const imageMap = {};
